{
  "name": "@hera/agent",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build --compile --outfile hera-agent.exe src/index.ts"
  },
  "dependencies": {
    "@hera/b1": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}
