# Dev only (vite dev mode). Tenant subdomains need a base domain whose parent-domain cookie
# is shared across subdomains — `localhost` is not (it behaves like a TLD), `lvh.me` is.
# lvh.me + *.lvh.me resolve to 127.0.0.1. Open the app at http://lvh.me:5173.
VITE_APP_BASE_DOMAIN=lvh.me
